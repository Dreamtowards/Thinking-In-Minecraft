# docs-v2 · 《Minecraft设计思想》新版

旧版 `docs/`（Grok 撰写）存在大量幻觉与过度压缩的黑话，难以挽救。本文件夹是重写版的工作目录：内容全部重写，标题与小节参考现有目录（或合理调整）。

- 章节任务源仍是根目录 `PLAN.md`；口径与体例见旧版 `docs/prelude/method.mdx`。
- 写完每一章后复查，通过后再写下一章。
- 全部完成后再整体迁入站点（替换 `docs/`），届时再补各部 `meta.json` 与部文件夹。
